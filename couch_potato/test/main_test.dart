// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility in the flutter_test package. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.



void main() async {
  /*
  WidgetsFlutterBinding.ensureInitialized();
  await Firebase.initializeApp();
  runApp(MyApp());

  testWidgets('MyApp should build without crashing', (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp());
    expect(find.byType(MaterialApp), findsOneWidget);
  });

  testWidgets('MyApp title should match', (WidgetTester tester) async {
    await tester.pumpWidget(const MyApp());
    expect(find.text('COUCH POTATO'), findsOneWidget);
  });

  testWidgets('MyHome should build without crashing', (WidgetTester tester) async {
    await tester.pumpWidget(const MaterialApp(home: MyHome()));
    expect(find.byType(Scaffold), findsOneWidget);
  });

  testWidgets('MyHome should have an AppBar', (WidgetTester tester) async {
    await tester.pumpWidget(const MaterialApp(home: MyHome()));
    expect(find.byType(AppBar), findsOneWidget);
  });

  testWidgets('MyHome should have a PageView', (WidgetTester tester) async {
    await tester.pumpWidget(const MaterialApp(home: MyHome()));
    expect(find.byType(PageView), findsOneWidget);
  });

  testWidgets('MyHome should have a bottomNavigationBar', (WidgetTester tester) async {
    await tester.pumpWidget(const MaterialApp(home: MyHome()));
    expect(find.byType(BottomNavigationBar), findsOneWidget);
  });
*/

}

